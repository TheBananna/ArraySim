#pragma once
#include "Eigen/Core"

//all vectors are column vectors unless otherwise stated to simplify doing rotation matricies
typedef Eigen::Matrix<double, 3, 1> vec3d;
typedef Eigen::Matrix<float, 3, 1> vec3f;
typedef Eigen::Matrix<int, 3, 1> vec2i;